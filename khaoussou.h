// Pour la determination de la longueur d'une chaine de caractere
int chaine(char ch[]);

//La suppression du nombre d'espace en debut de phrase
void espace(char phrase[]);

//La suppression du nombre d'espace en milieu
//char espace_milieu(char phrase[]);

// Verfication d'une lettre majuscule
int majuscule(char phrase[]);

// Verification des inintials
int initial(char numb[]);

//Pour la suppression des espaces dans une chaine
void espace(char numb[]);

//Pour verifier le nombre de chiffre d'un numero
int nbr_chiffre(char numb[]);

// Pour extraire un numero
void retirer(char val[], char numb[], int debut, int fin);

// Pour le traitement
void traitement();

//Pour  la matrice
void Matrice();

// Pour le nombre de carctere
int caractere(char numb[]);

