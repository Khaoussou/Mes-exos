//Pour remplir les information des etudaints
//void Saisi_etudiant();

// Pour la longueuer de la chiane
int chaine(char ch[]);

// Pour la conversion de ';' en '.'
void conversion(char note []);

// Pour le controle de la saisi
float control_saisi(char note []);

// Pour le remplissage et l'affichage du tableau
void afficche (char tableau [] );

//Pour la validé de numero
int num_valide (char ch[]);

//Pour l'affichage du numero
char numero(char telephone[]);